<x-app-layout>
    <x-slot name="header"><div class="flex items-center justify-between"><h2 class="text-xl font-semibold leading-tight text-gray-800 dark:text-gray-200">Task Detail</h2><div class="flex gap-3"><a href="{{ route('tasks.subtasks.create', $task) }}" class="rounded-md bg-slate-600 px-4 py-2 text-sm font-semibold text-white">Add Subtask</a>@if ($task->status !== 'Completed')<form method="POST" action="{{ route('tasks.complete', $task) }}">@csrf @method('PATCH')<button class="rounded-md bg-green-600 px-4 py-2 text-sm font-semibold text-white">Mark Completed</button></form>@endif<a href="{{ route('tasks.edit', $task) }}" class="rounded-md bg-indigo-600 px-4 py-2 text-sm font-semibold text-white">Edit</a><form method="POST" action="{{ route('tasks.destroy', $task) }}" onsubmit="return confirm('Delete this task?')">@csrf @method('DELETE')<button class="rounded-md bg-red-600 px-4 py-2 text-sm font-semibold text-white">Delete</button></form></div></div></x-slot>
    <div class="py-12"><div class="mx-auto max-w-3xl sm:px-6 lg:px-8"><div class="bg-white p-6 shadow-sm sm:rounded-lg dark:bg-gray-800"><dl class="grid gap-6 text-sm"><div><dt class="font-medium text-gray-500">Task</dt><dd class="mt-1 text-lg font-semibold text-gray-900 dark:text-gray-100">{{ $task->title }}</dd></div>@if ($task->parentTask)<div><dt class="font-medium text-gray-500">Parent Task</dt><dd class="mt-1"><a href="{{ route('tasks.show', $task->parentTask) }}" class="font-medium text-indigo-600">{{ $task->parentTask->title }}</a></dd></div>@endif<div><dt class="font-medium text-gray-500">Description</dt><dd class="mt-1 whitespace-pre-line text-gray-900 dark:text-gray-100">{{ $task->description ?: 'No description provided.' }}</dd></div><div class="grid gap-6 sm:grid-cols-2"><div><dt class="font-medium text-gray-500">Priority</dt><dd class="mt-1 text-gray-900 dark:text-gray-100">{{ $task->priority }}</dd></div><div><dt class="font-medium text-gray-500">Status</dt><dd class="mt-1 text-gray-900 dark:text-gray-100">{{ $task->status }}</dd></div><div><dt class="font-medium text-gray-500">Assigned To</dt><dd class="mt-1 text-gray-900 dark:text-gray-100">{{ $task->assignedUser?->name ?? 'Unassigned' }}</dd></div><div><dt class="font-medium text-gray-500">Created By</dt><dd class="mt-1 text-gray-900 dark:text-gray-100">{{ $task->creator->name }}</dd></div><div><dt class="font-medium text-gray-500">Estimated Hours</dt><dd class="mt-1 text-gray-900 dark:text-gray-100">{{ $task->estimated_hours ?? '—' }}</dd></div><div><dt class="font-medium text-gray-500">Actual Hours</dt><dd class="mt-1 text-gray-900 dark:text-gray-100">{{ $task->actual_hours ?? '—' }}</dd></div></div></dl><div class="mt-8 border-t border-gray-200 pt-6 dark:border-gray-700"><div class="flex items-center justify-between"><h3 class="font-semibold text-gray-900 dark:text-gray-100">Subtasks ({{ $task->subtasks->count() }})</h3><a href="{{ route('tasks.subtasks.create', $task) }}" class="text-sm font-medium text-indigo-600">Add Subtask</a></div><ul class="mt-4 divide-y divide-gray-200 dark:divide-gray-700">@forelse ($task->subtasks as $subtask)<li class="flex items-center justify-between gap-4 py-3"><a href="{{ route('tasks.show', $subtask) }}" class="text-sm font-medium text-indigo-600">{{ $subtask->title }}</a><span class="text-sm text-gray-500">{{ $subtask->status }} · {{ $subtask->assignedUser?->name ?? 'Unassigned' }}</span></li>@empty<li class="py-3 text-sm text-gray-500">No subtasks yet.</li>@endforelse</ul></div><div class="mt-6"><a href="{{ route('projects.show', $task->project) }}" class="text-sm font-medium text-indigo-600">Back to project</a></div></div></div></div>
</x-app-layout>
